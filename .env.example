# Strapi CMS Configuration
# URL of the Strapi backend (no trailing slash)

# Build-time only (private) - used to fetch landing page data during build
STRAPI_URL=http://localhost:1337

# Client-side (public) - used for form submissions from browser
PUBLIC_STRAPI_URL=http://localhost:1337

# Blog URL (public) - used for linking to the blog
PUBLIC_BLOG_URL=http://localhost:5173/
